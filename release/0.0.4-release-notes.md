# EnvOps v0.0.4 Release Notes

## Highlights

1. 后端 release gate 与本地启动入口已改为 reactor-safe 命令。
2. 前端 targeted unit gate 已有独立脚本，不再串跑无关 spec。
3. 完整 `pnpm --dir frontend test:unit` 已恢复绿色基线。
4. 系统账号密码已改为哈希存储与校验。
5. Deploy 创建契约已收敛到真实执行参数，不再对外承诺 `deployDir`。
6. Task Center 已明确降级为 deploy-only 队列视图。
7. Traffic 页面与接口已明确 skeleton / not-ready 边界，不再把占位能力包装成真实切流。
8. 资产中心当前仅支持主机、凭据、分组、标签，不支持数据库资源或数据库实例纳管。

## Deferred scope

1. Traffic 真实外部网关接通仍不在 `v0.0.4` 范围内。
2. Task Center 仍不是跨域统一队列。
3. Deploy 大规模主机检索和更深执行器增强继续延期。

## Verification

- `bash backend/scripts/test-envops-boot.sh`
- `pnpm --dir frontend test:unit`
- `pnpm --dir frontend typecheck`
- `pnpm --dir frontend build`
- `pnpm --dir frontend build:test`
- `pnpm --dir frontend exec oxlint .`
- `pnpm --dir frontend exec eslint .`
- Full engineering gate completed with all commands exiting `0`.
- Local smoke verified backend starts on `http://127.0.0.1:18080`, frontend test mode proxies to `http://127.0.0.1:18080`, and `envops-admin / EnvOps@123` can log in locally.
- Browser smoke verified Deploy 创建表单没有 `deployDir`，Task Center 文案明确为 deploy-only，Traffic 页面显示 not-ready warning，动作按钮禁用。
- README、CHANGELOG、release notes 与 `0.0.4` 版本文档已完成对齐。
